fn main() {
    let x = 5;
    let y = 10;
    let z = 20;

    println!("The sum of {} and {} is {}", x, y, x + y);
    println!("The product of {} and {} is {}", x, y, x * y);

    // Example: This code will print "Hello, World!" to the console
}
